-- ═══════════════════════════════════════════════════════════════════
-- Хранение API-ключей облачных провайдеров (только через админку).
-- Ключи шифруются на стороне приложения перед сохранением.
-- ═══════════════════════════════════════════════════════════════════

ALTER TABLE model_providers
ADD COLUMN IF NOT EXISTS encrypted_api_key TEXT;

COMMENT ON COLUMN model_providers.encrypted_api_key IS 'Зашифрованный API-ключ (Fernet). Задаётся через веб-интерфейс, не через .env';
